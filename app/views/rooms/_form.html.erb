<%= form_with(model: [room, message], id: "message_form", html: { class: "message-form-bar" }) do |f| %>
  <%= f.text_field :content, autocomplete: "off", class: "message-input" %>
  <%= f.submit "送信", class: "message-submit" %>
<% end %>
